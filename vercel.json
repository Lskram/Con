{
  "buildCommand": "pip install -r requirements.txt && npm install && npm run tailwind:build && python3 manage.py collectstatic --noinput",
  "rewrites": [
    {
      "source": "/static/(.*)",
      "destination": "/staticfiles/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
