{
  "buildCommand": "python -m pip install -r requirements.txt && npm install && npm run tailwind:build && python manage.py collectstatic --noinput",
  "rewrites": [
    {
      "source": "/static/(.*)",
      "destination": "/staticfiles/$1"
    },
    {
      "source": "/(.*)",
      "destination": "/api/index.py"
    }
  ]
}
